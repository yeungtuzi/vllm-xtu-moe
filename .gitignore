# ---- build artifacts -------------------------------------------------------
**/build/
dist/
*.egg-info/

# ---- python ---------------------------------------------------------------
**/__pycache__/
*.pyc
*.pyo

# ---- scratch / logs -------------------------------------------------------
*.log
*.tmp
logs/

# ---- measurement scratch (raw profiler traces are large; keep JSON/figures) --
report/*.nsys-rep
report/*.qdstrm
report/*.sqlite
report/st_native
report/st_nt
report/st_nt2
report/rd1

# ---- locally generated test checkpoints (scripts/make_tiny_mixtral.py) -----
/tiny-mixtral-vllm*/

# ---- model weights / datasets (never committed) ---------------------------
*.npz
*.safetensors
*.gguf

# ---- 调参产物(report/tuning)--------------------------------------
report/tuning/logs/
report/tuning/nsys/

# 重复/过时的源码副本与本地大 fixture(2026-09-13 按用户要求归档)
backup/
fixtures/*.npz

# 报告插图用的中文字体(8 MB,不入库;make_report_figures.py 会自动下载)
docs/figures/fonts/

# ---- 本会话 transcript 派生的回放数据集(绝不入库:仓库是公开的)-------------
# 内容 = 用户与 agent 的真实对话原文。用 `python scripts/make_dsh_replay.py` 现场重建。
report/tuning/replay/dsh_*.json
report/tuning/replay/dsh_*.jsonl

# ---- 内部开发文档(不发布)---------------------------------------------------
# GitHub **不支持"仓库内某目录私有"**:公开仓库里所有路径都对所有人可见。
# 所以内部文档(开发纪律、详细调优记录、内部报告、pptx/pdf)一律不进版本库,
# 只保留在本地工作副本 / 独立的私有仓库里。对外发布的是 docs/ 与 RELEASE_NOTES_*.md。
dev-docs/
